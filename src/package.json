{
  "name": "gonzazoid.path.js",
  "version": "0.0.1",
  "description": "",
  "main": "webpack.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ../lib && rm -rf ./build && rm -rf ../typings && mkdir ../lib && mkdir ./build && mkdir ../typings && tslint -c tslint.json 'index.ts' && tsc  -p 'tsconfig.json' && cp ./build/index.js ../lib/path.js && cp -R ./build/index.d.ts ../typings/"
  },
  "author": "Timur Appaev <alloaolla@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "tslint": "4.0.2",
    "typescript": "2.1.4"
  },
  "dependencies": {}
}
