{
  "name": "gonzazoid.path.js",
  "version": "0.0.1",
  "description": "",
  "main": "webpack.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ../lib && rm -rf ./build && mkdir ../lib && mkdir ./build && webpack --config webpack.js && cp ./build/path.js ../lib/ && cp -R ./build/typings ../"
  },
  "author": "Timur Appaev <alloaolla@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "ts-loader": "1.3.2",
    "tslint": "4.0.2",
    "tslint-loader": "3.2.1",
    "typescript": "2.1.4",
    "webpack": "2.3.2",
    "webpack-bundle-tracker": "0.1.0"
  },
  "dependencies": {}
}
